# 🧪 Actividad 3.2: Migración a base de datos gestionada con RDS

!!! danger "Pendiente — depende de Escaparate"
    Esta actividad depende de **Escaparate**, la aplicación transversal de los temas 3-7, todavía sin definir. No la publiques ni la des en clase tal cual — lo de abajo es el encargo para cuando se escriba de verdad, no contenido listo.

## Prompt pendiente

**Qué usamos**: Escaparate, migrando su base de datos actual a PostgreSQL en RDS.

**Idea central**: es la primera gran entrada de Escaparate en la nube — solo cambian los datos de conexión (`DB_HOST`, credenciales), no el código de la aplicación. Restricción explícita a mantener: ni una sola credencial escrita en el código de la aplicación.

**Referencia de la chuleta original** (documento de planificación anterior, revalidar el "hito H4" contra `curriculum.md` — probablemente RA4, «configuración y gestión de bases de datos en un entorno de nube»): fila 3.2.

Cuando Escaparate esté definido, reescribe esta actividad completa siguiendo el mismo patrón que el resto del módulo: Contexto, Qué vas a practicar, Requisitos previos, Parte A guiada + Parte B reto (a prueba de IA), Criterios de evaluación, Cierre con aviso de limpieza de recursos — y regenera sus plantillas docx.
