// Sustituye por la IP pública o el DNS público de tu instancia backend.
// Se deja como constante separada, en vez de escrita dentro de script.js,
// para que apuntar a otro backend (por ejemplo, uno de pruebas) no obligue
// a tocar la lógica de la página, solo esta línea.
const API_URL = "http://<ip-publica-backend>/api/resenas";
